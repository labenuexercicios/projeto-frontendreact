import {ItemCart} from './styled'

export const PageCart = (props) => { 
  return(
    <>
      {/* {props.contentCart.map((item) => {
        <ItemCart>
          <img src={item.image}/>
        </ItemCart>
      })} */}
    </>
  )
}